const LoadingIndicator = () => (
    <div className="flex items-center justify-center h-screen">
      <div className="text-teal animate-pulse">Loading...</div>
    </div>
);

export default LoadingIndicator;